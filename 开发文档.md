# DECISION CORE · 烂尾项目答辩决策推演终端 — 开发文档

> 用「多专家并发锐评 → 强化学习奖励收敛 → 3D 决策曲面」的硬核流程，
> 在赛博朋克监控大屏上**用数学证明：这趟答辩不该去**。
>
> 一句话结论：**A=0 不去**。把通宵爆肝的 48 工时还给期末与开源，ROI 由 −∞ 翻正。

---

## 0. TL;DR — 它到底算出了什么

| 维度 | 结论 |
| --- | --- |
| 团队真实坐标 | 代码质量 `C=0.1`、核心在校人员可用度 `F=0` |
| 去答辩的奖励 | `goReward(0.1, 0) = −285`（3D 曲面血红洼地 = 死局） |
| 不去的机会价值 | `+80`（复习期末 / 搞 GitHub 开源 / 保住头发） |
| RL 收敛结果 | `P(去)` 从初始 85% 的愧疚冲动，数百轮内坍缩到 `≈0%` |
| 四算法是否一致 | PPO / DPO / Q-Learning / RLHF 全部裁定 **不去** |

---

## 1. 目录结构

```
创客项目答辩计划设计/
├─ index.html            # ★ 主交付物：自包含单文件赛博朋克决策大屏（仅依赖 CDN）
├─ 开发文档.md           # 本文件
├─ package.json          # npm test 入口
├─ dev-server.mjs        # 零依赖静态服务器（本地预览用，非交付物的一部分）
├─ .claude/launch.json   # 预览配置（可选）
└─ tests/
   ├─ rl-core.js         # 强化学习核心引擎（UMD，单一事实源 / single source of truth）
   └─ rl-core.test.js    # TDD 规格（node:test），21 条断言
```

> **重要约定**：`index.html` 为满足「单文件」要求，**内联了 `tests/rl-core.js` 的逐字镜像**
> （在文件中搜索标记 `===== RL CORE MIRROR =====`）。
> 修改算法时，必须同时改动两处并重新 `npm test`，保持数学行为一致。

---

## 2. 快速开始

### 方式 A：直接双击打开（最省事）
`index.html` 是纯静态单文件，所有库走 CDN。**直接用浏览器打开即可**，无需本地服务器。
（需要联网拉取 Tailwind / ECharts / ECharts-GL / Chart.js 的 CDN。）

### 方式 B：本地服务器预览
```bash
node dev-server.mjs       # → http://localhost:4173
# 或者
npx serve .               # 任意静态服务器都行
```

### 方式 C：跑测试（TDD）
```bash
npm test                  # 等价于 node --test，自动发现 tests/*.test.js
npm run test:watch        # 改代码自动重跑
```

### 配置 DeepSeek（可选）
1. 在页面顶栏输入 `sk-` 开头的 DeepSeek API Key（仅存于浏览器内存，不落盘、不外传）。
2. 「思考模式」开关：开 = `deepseek-reasoner`（展示思考链）；关 = `deepseek-chat`（动态温度真实生效）。
3. 不想花 token？打开「DEMO / 离线锐评」开关，用内置毒舌库离线跑完整流程。

---

## 3. 架构设计：责任链（Chain of Responsibility）

整条推演流水线由 4 个 Handler 串成单向责任链，每个 Handler 处理完上下文 `ctx`
后向下游传递，并向「系统日志」终端与「流水线」状态灯汇报进度。

```
runPipeline(ctx)
   │
   ▼
┌──────────────────────────┐   ctx 透传   ┌──────────────────────────┐
│ BackgroundInjectionHandler│ ───────────▶ │   ExpertPanelHandler      │
│  ① 注入项目真实背景        │              │  ② Promise.all 并发三专家 │
└──────────────────────────┘              └──────────────────────────┘
                                                       │
                       ┌───────────────────────────────┘
                       ▼
            ┌──────────────────────┐   ctx   ┌──────────────────────┐
            │  RLSimulationHandler  │ ──────▶ │  VisualizationHandler │
            │  ③ 1000 轮 RL 收敛    │         │  ④ Chart.js + GL 曲面 │
            └──────────────────────┘         └──────────────────────┘
```

```js
class Handler {
  setNext(h) { this.next = h; return h; }                 // 链式拼装
  async handle(ctx) {                                       // 模板方法
    await this.process(ctx);
    return this.next ? this.next.handle(ctx) : ctx;
  }
  async process() {}                                        // 子类实现各阶段
}
// 组装：inject → experts → rl → viz
inject.setNext(experts).setNext(rl).setNext(viz);
await inject.handle(ctx);
```

`ctx` 携带：`apiKey / thinking / demo / signal`（输入）与
`expertScores / rl / convAt`（各阶段产出），实现阶段间解耦。

---

## 4. 第一阶段 · 多专家并发锐评

### 4.1 两步走
1. **背景注入**：把《项目真实背景》作为上下文喂给模型（`temperature=0.3`），让它先确认理解并点名最致命风险。
2. **并发锐评**：`Promise.all` 同时发起 3 路请求，让**同一个模型**通过不同 System Prompt
   「精神分裂」成三位专家：

| 专家 | 人设 | 动态温度 τ | 关注点 |
| --- | --- | --- | --- |
| 技术专家 | 毒舌架构师 | **1.5** | 代码连课设都不如，重构=拿生命做无用功 |
| 风控专家 | 合规诚信官 | **1.1** | 企业跑路后强行结题的合规 / 诚信 / 背锅风险 |
| 成本专家 | ROI 分析师 | **0.7** | 「通宵重构」vs「复习+开源」的投入产出比 |

### 4.2 思考模式与动态温度（重要工程取舍）
- **动态温度**：不同角色按上表注入不同 `temperature`——毒舌锐评要发散（高温），ROI 计算要稳准（低温）。
- **DeepSeek 的现实约束**：`deepseek-reasoner`（思考模式）**会忽略 `temperature` 参数**，由内部思考链自洽控制采样；
  `deepseek-chat` 才让温度真实生效。
- **本项目的处理**：思考模式开关二选一——
  - 开 → `deepseek-reasoner`，前端展示 `reasoning_content`（可折叠「思考链」），温度仅作展示；
  - 关 → `deepseek-chat`，上表的逐角色温度真实下发。
  请求体始终携带 `temperature`（对 reasoner 无害），UI 实时提示当前模式下温度是否生效。

### 4.3 健壮性
- 每路请求独立 `try/catch`，单点失败自动降级到**内置毒舌兜底文案**，不阻断后续 RL 与可视化。
- 用正则解析每位专家末行的 `GO_SCORE: <0-100>`，聚合成「专家共识指数」，与 RL 结果交叉印证。
- API Key 仅存于内存变量，绝不写入 URL / localStorage / 日志。

---

## 5. 第二阶段 · 强化学习奖励收敛

### 5.1 决策建模（单状态 → 退化为二臂老虎机）
- **状态**（固定）：`C=0.1, F=0`
- **动作**：`A=1` 去 / `A=0` 不去
- **去的奖励曲面**：`R(去) = 100·C·F − 150·(1−C) − 100·(1−F) − 50`，在 `(0.1, 0)` 处 `= −285`
- **不去的奖励**：`STAY_REWARD = +80`（机会价值）
- **初始策略**：`P(去)=0.85`（建模「愧疚冲动」先验，让收敛曲线从负数深渊起步更有戏剧性）

### 5.2 四种对齐算法（横向对比）
> 全部基于确定性 PRNG `mulberry32(seed)`，**可复现、可测试**。报告的「期望收益」统一为
> `E[R] = P(去)·R(去) + (1−P(去))·R(不去)`，便于四条曲线同台对比。

| 算法 | 思想 | 关键更新式 |
| --- | --- | --- |
| **PPO** | 裁剪策略梯度（信任域） | `θ += clip(lr·A·∂logπ, −ε, ε)`，优势 `A=r−baseline` |
| **DPO** | 直接偏好优化，偏好对固定为「不去 ≻ 去」，参考策略均匀 | `∂L/∂θ = β·(1−σ(−βθ))`，沿负梯度把 `P(去)` 压向 0 |
| **Q-Learning** | 表格型 TD，ε-贪心 + softmax 导出概率 | `Q[a] += α·(r − Q[a])`，`Q[去]→−285, Q[不去]→80` |
| **RLHF** | 奖励模型 + PPO，真实奖励经含高斯噪声的 RM 估计 | 同 PPO，但 `adv = (r + 噪声) − baseline`，路径更抖仍收敛 |

> DPO 的梯度推导：设 `π(去)=σ(θ)`、参考策略均匀，则偏好对（不去 ≻ 去）的对数比 margin `= −θ`，
> `L = −log σ(−βθ)`，求导即得上表更新式。

### 5.3 为什么必然收敛到「不去」
无论采样到哪个动作，优势/偏好梯度都把策略往「不去」推：
- 采到「去」：`r=−285 ≪ baseline`，优势为负 → 压低 `P(去)`；
- 采到「不去」：`r=+80 ≫ baseline`，优势为正、但 `∂logπ(不去)/∂θ = −P(去)` → 同样压低 `P(去)`。

因此 `P(去)` 单调坍缩到 `≈0`，期望收益从负数深渊回升并稳定在 `+80` 平台。

---

## 6. 第三阶段 · 数据可视化

### 6.1 二维收敛（Chart.js）
- **期望收益收敛图**：4 条算法曲线 + 一条 `R(不去)=+80` 虚线参照，展示从负数深渊爬升到 +80 平台。
- **动作概率坍缩图**：4 条 `P(去) %` 曲线，从 85% 一路坍缩到 ≈0%。
- 千点序列降采样到 ~200 点绘制，保证流畅。

### 6.2 三维曲面（ECharts-GL）
- 用 `series.type='surface'` 的 **`equation`** 直接由奖励公式驱动渲染（规避网格数据格式风险）：
  ```js
  equation: {
    x: { min: 0, max: 1, step: 1/40 },
    y: { min: 0, max: 1, step: 1/40 },
    z: (x, y) => RLCore.goReward(x, y),   // 100xy − 150(1−x) − 100(1−y) − 50
  }
  ```
- `visualMap` 按 Z 着色：荧光绿（高收益）→ 黄（临界）→ 血红（死局），范围 `[−300, 50]`。
- 在 `(X=0.1, Y=0, Z=−285)` 用 `scatter3D` 标注**巨型红点**，标签：
  `【咱们团队目前的位置：死局 Z=−285】`，通过对 `series[1]` 做 merge-`setOption`
  实现 0.6s 周期的**呼吸闪烁**（仅更新散点，不重置相机视角）。

---

## 7. 测试驱动开发（TDD）

先写规格再写实现：`tests/rl-core.test.js` 用 Node 内置 `node:test` / `node:assert`，**零三方依赖**。

```bash
npm test     # 21 项断言全绿
```

覆盖范围：
- **数学基元**：`sigmoid` 单调性、`logit∘sigmoid` 互逆、`mulberry32` 确定性与值域 `[0,1)`。
- **奖励模型**：`goReward` 在 `(0.1,0)=−285`、`(0,0)=−300`、`(1,1)=50` 等关键点精确取值；`STAY_REWARD=80`。
- **3D 曲面数据**：网格规模 `(steps+1)²`、全局最低 `−300` / 最高 `50`、死局点 `−285`。
- **四个训练器**（通用契约）：
  - 输出结构与**确定性**（同种子完全可复现）；
  - `P(去)` 从 85% 收敛到 `<5%`（PPO / Q-Learning 进一步 `<1%`）；
  - 期望收益从负起步、回升并稳定在 `+80` 附近。
- **`runAll`**：一次性产出四算法结果用于对比。

> 浮点注意：`1−0.1` 在 IEEE754 下非精确，断言用 `approx(±1e-6)` 而非严格相等。

---

## 8. 配置接口一览

| 配置项 | 位置 | 说明 |
| --- | --- | --- |
| API Key | 顶栏输入框 | DeepSeek `sk-...`，仅内存保存 |
| 思考模式 | 顶栏开关 | reasoner（思考链） / chat（动态温度） |
| DEMO 模式 | 顶栏开关 | 离线毒舌库，不消耗 token |
| 角色温度 | `EXPERTS[].temp` | 1.5 / 1.1 / 0.7，可改 |
| RL 轮次 / 初始概率 | `RLCore.runAll({episodes, initPGo})` | 默认 1000 / 0.85 |
| 奖励常数 | `RLCore.STAY_REWARD`、`goReward` | 改这里同时改镜像并重测 |
| API 端点 | `callDeepSeek` 内 | `https://api.deepseek.com/chat/completions`（OpenAI 兼容） |

---

## 9. 已知限制

- **CDN 依赖联网**：离线环境下 Tailwind/ECharts/Chart.js 无法加载；可自行下载到本地改为相对路径。
- **WebGL 要求**：3D 曲面需浏览器支持 WebGL；老旧显卡/远程桌面可能降级或黑屏。
- **reasoner 不吃温度**：见 §4.2，这是 DeepSeek 的产品约束，非本项目 Bug。
- **跨域**：DeepSeek 接口支持浏览器直连；如遇企业代理拦截，请改用后端转发。
- **镜像同步**：算法逻辑存在「`rl-core.js` ↔ index.html 内联」两份，改动需手动同步（见 §1）。

---

## 10. 免责声明（彩蛋）

本终端是一台**理性止损机**，不是甩锅工具。它的唯一主张：
**沉没成本不构成继续亏损的理由**。
数学已经把话说尽——`R(去) = −285`。剩下的，交给你的腰和发际线定夺。

`// Decision Core © 你的团队 · 拒绝为屎山陪葬`
